const minus = num1 - num2;
const plus = num1 + num2;
const divide = num1 / num2;
const times = num1 * num2;

module.exports {
    minus: function(mathCheck){
        return num1 - num2
    },
    plus: function(mathCheck){
        return  num1 + num2
    },
    
}